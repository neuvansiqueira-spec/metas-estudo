name: Testar e publicar Resumo do Dia direto V251

on:
  push:
    branches: [main]
    paths:
      - daily-summary-elegant-direct-v251.css
      - docs/daily-summary-elegant-direct-v251.css
      - scripts/apply-daily-summary-elegant-direct-v251.mjs
      - tests/daily-summary-elegant-direct-v251.test.mjs
      - .github/workflows/test-daily-summary-elegant-direct-v251.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-and-publish:
    if: ${{ false }} # V316: fluxo legado desativado
    runs-on: ubuntu-latest
    steps:
      - name: Baixar repositório
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Configurar Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22

      - name: Aplicar V251 pelo mesmo caminho das correções anteriores
        run: node scripts/apply-daily-summary-elegant-direct-v251.mjs

      - name: Validar sintaxe
        run: |
          node --check scripts/apply-daily-summary-elegant-direct-v251.mjs
          node --check service-worker-v236.js
          node --check docs/service-worker-v236.js

      - name: Executar testes
        run: node --test tests/daily-summary-elegant-direct-v251.test.mjs

      - name: Validar arquivos publicados
        run: |
          git diff --check
          cmp index.html docs/index.html
          cmp daily-summary-elegant-direct-v251.css docs/daily-summary-elegant-direct-v251.css
          cmp service-worker-v236.js docs/service-worker-v236.js

      - name: Publicar V251
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html docs/index.html service-worker-v236.js docs/service-worker-v236.js
          if ! git diff --cached --quiet; then
            git commit -m "Publica correção direta V251 no Resumo do Dia"
            git push origin HEAD:main
          fi
