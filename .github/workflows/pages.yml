name: Liberar Publicação GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  release-lock:
    runs-on: ubuntu-latest
    steps:
      - name: Confirmar publicação legacy V266
        run: echo "Liberação do bloqueio de publicação das relações e da busca global V266; GitHub Pages continua publicado pela raiz da main."
