name: Paleta da Central de Metas V245 — pausado

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow V245 pausado para correção do alvo visual."
